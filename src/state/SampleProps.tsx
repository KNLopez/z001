const sampleFormProps: any[] = [];

const ModalState = {
  show: false,
  field: "",
};

const formProps = {
  title: "Form title",
  formNumber: "FORM0001",
  status: "DRAFT",
  elements: sampleFormProps,
  modalState: ModalState,
};

export default formProps;
