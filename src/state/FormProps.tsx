const FormProps: any[] = [];

const ModalState = {
  show: false,
  field: "",
};

const formProps = {
  title: "",
  formNumber: "",
  status: "",
  elements: FormProps,
  modalState: ModalState,
  currentField: "",
};

export default formProps;
